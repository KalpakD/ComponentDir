import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {flex: 1, backgroundColor: '#ECF0F1'},
  headerText: {fontSize: 16, fontWeight: '500', padding: 16},
});

export default styles;
